<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="tab_text" parent="@android:style/TextAppearance.Widget.TabWidget">
        <item name="android:fontFamily">@font/orbitron_semibold</item>
    </style>

    <!--Custom themes here
            colorPrimary is for main background color and buttons color
            colorSecondary is button text color
            colorAccent is cells backgroundColor and buttons background color

            text color is colorAccent when background is

    -->

    <style name="AppThemeWaluigi" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:forceDarkAllowed" tools:targetApi="q"> false </item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/harmonia_clear</item>
        <item name="colorSecondary">@color/white</item>
        <item name="colorAccent">@color/harmonia_dark</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/harmonia_dark</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="AppThemeWario" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:forceDarkAllowed" tools:targetApi="q"> false </item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/harmonia_clear</item>
        <item name="colorSecondary">@color/white</item>
        <item name="colorAccent">@color/harmonia_dark</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/harmonia_dark</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="AppThemeMario" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:forceDarkAllowed" tools:targetApi="q"> false </item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/harmonia_clear</item>
        <item name="colorSecondary">@color/white</item>
        <item name="colorAccent">@color/harmonia_dark</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/harmonia_dark</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="AppThemeLuigi" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:forceDarkAllowed" tools:targetApi="q"> false </item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/harmonia_clear</item>
        <item name="colorSecondary">@color/white</item>
        <item name="colorAccent">@color/harmonia_dark</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/harmonia_dark</item>
        <!-- Customize your theme here. -->
    </style>
</resources>